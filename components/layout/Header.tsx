"use client"

import FloatingNavbar from "@/components/ui/floating-navbar"

export default function Header() {
  // Only return the floating navbar, remove the duplicate header
  return <FloatingNavbar />
}
